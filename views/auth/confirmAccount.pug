extends ../layout/index

block contenido
    input(type="hidden" name="_csrf" value= csrfToken)
    div.py-10.bg-white
        h1.text-4xl.my-10.font-extrabold.text-center.text-action Bienes 
            span.font-normal.text-hoverAction Raices
        h2.text-center.text-2xl.font-extrabold.text-secondaryText.mt-4= page

    div(class="bg-formBackground shadow-lg py-8 px-6 rounded-lg max-w-md mx-auto mt-6 border border-gray-200")
        if mesage
            p(class= error ? 'bg-error' : 'bg-success').py-3.px-4.rounded-md.w-full.font-bold.text-center.text-white.uppercase= mesage

        if !error 
            a(href="/auth/login" 
            class="w-full inline-block bg-action text-white hover:bg-hoverAction transition duration-300 ease-in-out font-bold py-3 text-center rounded-lg mt-4") Ya puedes Iniciar Sesión

