extends ../layout/index

block contenido
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center.text-action Bienes
            span.font-normal.text-hoverAction Raíces  
        h2.text-center.text-2xl.font-extrabold= page

        if errors 
            div(class="max-w-md mx-auto my-10")
                each error in errors
                    p.bg-red-600.text-white.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg

        .mt-8.mx-auto.max-w-md 
            .bg-formBackground.py-8.px-4.shadow
                form.space-y-5(method="POST" novalidate) 
                    input(type="hidden" name="_csrf" value= csrfToken)
                    div 
                        label.block.text-sm.uppercase.text-secondaryText.mb-3.font-bold(for="new_password") Ingresa tu nueva contraseña:
                        input#new_password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="mínimo de 8 carácteres" type="password" name="new_password") 
                    
                        label.block.text-sm.uppercase.text-secondaryText.mb-3.font-bold(for="confirm_new_password") Ingresa tu nueva contraseña:
                        input#confirm_new_password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="deberá coincidir con la anterior" type="password" name="confirm_new_password") 

                    input(class="w-full bg-action hover:bg-hoverAction text-white font-bold py-3 cursor-pointer" type="submit" value="Actualizar Contraseña")
